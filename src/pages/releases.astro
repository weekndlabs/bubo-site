---
import '../styles/tokens.css';

// Bubo ships from this site: the .dmg files live in public/ and are committed
// per release, so only the newest entry carries download buttons.
const app = 'https://app.bubo.weekndlabs.com';
const dmgArm = '/Bubo-apple-silicon.dmg';
const dmgIntel = '/Bubo-intel.dmg';

// The real releases. Notes are written from what actually shipped, newest first.
// change.k tags a diff (new / changed / fix); no k = a plain line (e.g. a first release).
const releases = [
  {
    v: '1.4',
    date: '28 Jul 2026',
    latest: true,
    lede: 'The panel got out of its own way. It opens flat under the menu bar and as tall as the screen allows, and the readings inside it now say what they are measured against.',
    changes: [
      { k: 'new', t: 'Check for Updates in the right-click menu, comparing the latest release against the build you are running' },
      { k: 'new', t: 'Report a bug opens a report form with your Bubo build, chip, RAM and macOS version already filled in' },
      { k: 'changed', t: 'Panel opens flat under the menu bar and as tall as the screen allows, instead of a fixed height that hid the lower half behind a scroll' },
      { k: 'changed', t: 'Efficiency and performance rows draw a bar per core and end in that tier\'s usage and speed; the separate core row is gone' },
      { k: 'changed', t: 'Fan reads against its own floor and ceiling, on a row under the temperatures' },
      { k: 'changed', t: 'Memory leads with used against total, and DRAM bandwidth moved above the bar, out of the app / wired / compressed / cached list' },
      { k: 'changed', t: 'Power rails split the draw by rail on their own bar, next to battery' },
    ],
    download: true,
  },
  {
    v: '1.3',
    date: '27 Jul 2026',
    lede: 'Bubo names the cause now. The panel used to show readings and leave the interpreting to you. It now says what is likely wrong and one thing to do about it, in a sentence.',
    changes: [
      { k: 'new', t: 'Diagnosis banner: the likely cause and one suggested action, in plain words' },
      { k: 'new', t: 'System uptime in the panel header' },
      { k: 'changed', t: 'Panel draws a solid background instead of the popover material' },
      { k: 'fix', t: 'Memory matches Activity Monitor, per app and system used' },
      { k: 'fix', t: 'Battery shows the charge macOS shows, not the raw mAh ratio' },
      { k: 'fix', t: 'A busy app in front no longer hides a runaway in the background' },
      { k: 'fix', t: 'Memory pressure follows the OS verdict instead of two guessed thresholds' },
    ],
  },
  {
    v: '1.2',
    date: '26 Jul 2026',
    lede: 'Clearer at a glance. Full-word CPU tiers, a plain-language throttling badge, and a memory bar that shows what your RAM is actually spent on.',
    changes: [
      { k: 'changed', t: 'CPU cores named efficiency / performance, one per row, with temp, hotspot and pressure under the bar' },
      { k: 'changed', t: 'Status badge reads throttling in plain words when the Mac runs hot, not "thermal serious"' },
      { k: 'new', t: 'Memory broken down into app, wired, compressed, cached and free' },
    ],
  },
  {
    v: '1.1',
    date: '25 Jul 2026',
    lede: 'Intel is in. Bubo now ships a native build for Intel Macs alongside Apple Silicon.',
    changes: [
      { k: 'new', t: 'Native Intel build, Bubo-intel.dmg' },
    ],
  },
  {
    v: '1.0',
    date: '24 Jul 2026',
    lede: 'First light. The whole panel in your menu bar, and one click to quit the app dragging your Mac.',
    changes: [
      { t: 'Top apps: the eight heaviest processes, grouped per app, one-click quit' },
      { t: 'CPU, memory, GPU and fan, battery, network and disk' },
      { t: 'Power rails and temperatures on Apple Silicon' },
      { t: 'Purge disk cache, or quit every heavy app at once' },
      { t: 'Native Apple Silicon, no daemon, no sudo' },
    ],
  },
];
---

<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Bubo — releases</title>
  <meta name="description" content="Every Bubo release, logged. What changed in each build, newest first, and where to download it for Apple Silicon or Intel." />
  <link rel="canonical" href="https://bubo.weekndlabs.com/releases" />
  <link rel="icon" type="image/png" href="/logo.png" />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://bubo.weekndlabs.com/releases" />
  <meta property="og:site_name" content="Bubo" />
  <meta property="og:title" content="Bubo — releases" />
  <meta property="og:description" content="Every Bubo release, logged. Newest first, with the download for your chip." />
  <meta property="og:image" content="https://bubo.weekndlabs.com/og.png" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:image" content="https://bubo.weekndlabs.com/og.png" />
  <meta name="theme-color" content="#12141F" media="(prefers-color-scheme: dark)" />
  <meta name="theme-color" content="#FAF8F3" media="(prefers-color-scheme: light)" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,500;12..96,600;12..96,700;12..96,800&family=Inter:wght@400;500;600&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet" />
  <script is:inline>
    const t = localStorage.getItem('bubo-theme');
    if (t) document.documentElement.style.colorScheme = t;
  </script>
</head>
<body>
  <header class="menubar">
    <a class="mb-brand" href="/" aria-label="Bubo, live readout">
      <span class="mb-dot" aria-hidden="true"></span>
      <span class="mb-read"><b>CPU</b> 22%&nbsp;&nbsp;<b>RAM</b> 78%</span>
    </a>
    <nav class="mb-nav">
      <a class="mb-sec" href="/#features">Features</a>
      <a class="mb-sec" href="/#install">Install</a>
      <a href="/releases" aria-current="page">Releases</a>
      <a class="mb-in" href={app}>Sign&nbsp;in</a>
      <button class="mb-theme" type="button" aria-label="Toggle light or dark theme">☾</button>
    </nav>
  </header>

  <main>
    <section class="rel-hero">
      <p class="eyebrow">changelog</p>
      <h1>Every build, logged.</h1>
      <p class="lede">
        {releases.length} releases so far, newest first. Bubo watches all night;
        this is what it shipped. Grab the latest for Apple&nbsp;Silicon or Intel.
      </p>
    </section>

    <section class="timeline">
      {releases.map((r) => (
        <article class:list={['release', { 'is-latest': r.latest }]}>
          <span class="rel-dot" aria-hidden="true"></span>
          <div class="rel-body">
            <header class="rel-head">
              <span class="rel-ver">v{r.v}</span>
              <span class="rel-date">{r.date}</span>
              {r.latest && <span class="rel-badge">Latest</span>}
            </header>

            <p class="rel-lede">{r.lede}</p>

            <ul class="rel-log">
              {r.changes.map((c) => (
                <li class="rel-row">
                  {c.k
                    ? <span class:list={['rel-tag', `rel-tag--${c.k}`]}>{c.k}</span>
                    : <span class="rel-bullet" aria-hidden="true">·</span>}
                  <span>{c.t}</span>
                </li>
              ))}
            </ul>

            {r.download && (
              <div class="rel-dl">
                <a class="btn-dl" href={dmgArm} download rel="noopener">Apple&nbsp;Silicon <span class="dl-ext">.dmg</span></a>
                <a class="btn-dl btn-dl-alt" href={dmgIntel} download rel="noopener">Intel&nbsp;Chip <span class="dl-ext">.dmg</span></a>
              </div>
            )}
          </div>
        </article>
      ))}
    </section>
  </main>

  <footer class="footer">
    <div class="foot-brand">
      <img src="/logo.png" alt="" width="40" height="40" />
      <div>
        <div class="foot-name">Bubo</div>
        <p class="foot-tag">The genus of the eagle-owl. It watches all night and misses nothing.</p>
      </div>
    </div>
    <nav class="foot-links">
      <a href="/">Home</a>
      <a href="/#install">Download</a>
      <a href={app}>Bubo ID</a>
    </nav>
    <p class="foot-legal">© 2026 fajarhide · Apple Silicon and Intel</p>
  </footer>

  <script>
    // Theme toggle: flip color-scheme on <html> and remember it (shared with home).
    (() => {
      const root = document.documentElement;
      const btn = document.querySelector('.mb-theme');
      const media = matchMedia('(prefers-color-scheme: dark)');
      const current = () => localStorage.getItem('bubo-theme') || (media.matches ? 'dark' : 'light');
      const paint = () => { btn.textContent = current() === 'dark' ? '☾' : '☀'; };
      paint();
      media.addEventListener('change', paint);
      btn?.addEventListener('click', () => {
        const next = current() === 'dark' ? 'light' : 'dark';
        localStorage.setItem('bubo-theme', next);
        root.style.colorScheme = next;
        paint();
      });
    })();
  </script>

  <style is:global>
    /* the changelog is a reading column, narrower than the marketing pages */
    :root { --wrap: 760px; }

    * { box-sizing: border-box; }
    html { scroll-behavior: smooth; }
    body {
      margin: 0; background: var(--night); color: var(--text);
      font-family: var(--body); font-size: 17px; line-height: 1.6;
      -webkit-font-smoothing: antialiased; text-rendering: optimizeLegibility;
      overflow-x: hidden;
    }
    a { color: inherit; }
    h1 { font-family: var(--display); font-weight: 800; letter-spacing: -0.02em; margin: 0; }
    em { font-style: normal; color: var(--amber); }
    .eyebrow {
      font-family: var(--mono); font-size: 0.72rem; letter-spacing: 0.22em;
      text-transform: uppercase; color: var(--amber); margin: 0 0 1.1rem;
    }

    /* ---- menu bar (shared chrome) ---- */
    .menubar {
      position: sticky; top: 0; z-index: 50;
      display: flex; align-items: center; justify-content: space-between;
      height: 34px; padding: 0 clamp(14px, 4vw, 40px);
      background: light-dark(rgba(250, 248, 243, 0.72), rgba(18, 20, 31, 0.72));
      backdrop-filter: saturate(140%) blur(14px);
      -webkit-backdrop-filter: saturate(140%) blur(14px);
      border-bottom: 1px solid var(--line-soft);
    }
    .mb-brand { display: flex; align-items: center; gap: 8px; text-decoration: none; }
    .mb-dot {
      width: 9px; height: 9px; border-radius: 50%; background: var(--amber);
      box-shadow: 0 0 0 0 rgba(245, 184, 65, 0.5); animation: pulse 3.4s ease-out infinite;
    }
    .mb-read { font-family: var(--mono); font-size: 0.74rem; color: var(--muted); }
    .mb-read b { color: var(--faint); font-weight: 500; }
    .mb-nav { display: flex; align-items: center; gap: clamp(14px, 3vw, 28px); }
    .mb-nav a {
      font-family: var(--mono); font-size: 0.76rem; text-decoration: none;
      color: var(--muted); transition: color 0.18s;
    }
    .mb-nav a:hover, .mb-nav a[aria-current="page"] { color: var(--amber); }
    .mb-in {
      color: var(--text); border: 1px solid var(--line);
      border-radius: 7px; padding: 3px 10px; line-height: 1.5;
    }
    .mb-in:hover, .mb-nav a.mb-in:hover { border-color: var(--amber); color: var(--amber); }
    .mb-theme {
      font-family: var(--mono); font-size: 0.9rem; line-height: 1;
      background: none; border: 0; padding: 0; cursor: pointer;
      color: var(--muted); transition: color 0.18s;
    }
    .mb-theme:hover { color: var(--amber); }
    @keyframes pulse {
      0% { box-shadow: 0 0 0 0 rgba(245, 184, 65, 0.45); }
      70% { box-shadow: 0 0 0 7px rgba(245, 184, 65, 0); }
      100% { box-shadow: 0 0 0 0 rgba(245, 184, 65, 0); }
    }

    /* ---- release hero ---- */
    .rel-hero {
      max-width: var(--wrap); margin: 0 auto;
      padding: clamp(40px, 8vw, 88px) clamp(20px, 5vw, 40px) clamp(28px, 4vw, 44px);
      animation: rise 0.6s cubic-bezier(0.2, 0.7, 0.2, 1) both;
    }
    .rel-hero h1 { font-size: clamp(2.4rem, 6vw, 3.6rem); line-height: 1; }
    .rel-hero .lede { color: var(--muted); font-size: 1.08rem; max-width: 48ch; margin: 1.2rem 0 0; }

    /* ---- the watch log: a status dot on a vertical spine per release ---- */
    .timeline {
      position: relative; max-width: var(--wrap); margin: 0 auto;
      padding: clamp(8px, 2vw, 20px) clamp(20px, 5vw, 40px) clamp(56px, 9vw, 100px);
    }
    /* the spine: a hairline running through the dots */
    .timeline::before {
      content: ''; position: absolute;
      left: calc(clamp(20px, 5vw, 40px) + 6px); top: 14px; bottom: clamp(56px, 9vw, 100px);
      width: 1px; background: var(--line-soft);
    }
    .release { position: relative; padding-left: 34px; padding-bottom: clamp(34px, 5vw, 56px); }
    .release:last-child { padding-bottom: 0; }
    .rel-dot {
      position: absolute; left: 0; top: 7px; width: 13px; height: 13px; border-radius: 50%;
      background: var(--faint); box-shadow: 0 0 0 4px var(--night);
    }
    .is-latest .rel-dot { background: var(--amber); animation: pulse 3.4s ease-out infinite; }

    .rel-head { display: flex; align-items: baseline; gap: 12px; flex-wrap: wrap; }
    .rel-ver { font-family: var(--display); font-weight: 700; font-size: 1.5rem; letter-spacing: -0.01em; }
    .rel-date { font-family: var(--mono); font-size: 0.76rem; color: var(--faint); }
    .rel-badge {
      font-family: var(--mono); font-size: 0.58rem; font-weight: 600; letter-spacing: 0.08em;
      text-transform: uppercase; color: var(--amber); background: rgba(245, 184, 65, 0.13);
      padding: 3px 8px; border-radius: 20px;
    }
    .rel-lede { color: var(--muted); font-size: 1.02rem; margin: 12px 0 0; max-width: 56ch; }

    .rel-log { list-style: none; margin: 16px 0 0; padding: 0; }
    .rel-row {
      display: grid; grid-template-columns: auto 1fr; align-items: baseline; gap: 12px;
      padding: 6px 0; font-size: 0.96rem; color: var(--text);
    }
    .rel-tag {
      justify-self: start; font-family: var(--mono); font-size: 0.58rem; font-weight: 600;
      letter-spacing: 0.08em; text-transform: uppercase; padding: 3px 8px; border-radius: 20px;
      transform: translateY(-1px);
    }
    .rel-tag--new { color: var(--sage); background: color-mix(in srgb, var(--sage) 15%, transparent); }
    .rel-tag--fix { color: var(--clay); background: color-mix(in srgb, var(--clay) 15%, transparent); }
    .rel-tag--changed { color: var(--amber); background: rgba(245, 184, 65, 0.13); }
    .rel-bullet { color: var(--faint); font-family: var(--mono); }

    .rel-dl { display: flex; gap: 10px; margin-top: 20px; flex-wrap: wrap; max-width: 440px; }
    .btn-dl {
      flex: 1 1 150px; text-align: center;
      font-family: var(--mono); font-size: 0.86rem; text-decoration: none;
      color: var(--night); background: var(--amber); border: 1px solid transparent;
      border-radius: 10px; padding: 11px 18px;
      transition: background 0.18s, border-color 0.18s, color 0.18s, transform 0.1s;
    }
    .btn-dl:hover { background: var(--amber-bright); }
    .btn-dl:active { transform: translateY(1px); }
    .dl-ext { font-size: 0.78em; opacity: 0.7; }
    .btn-dl-alt { background: none; color: var(--text); border-color: var(--line); }
    .btn-dl-alt:hover { background: none; border-color: var(--amber); color: var(--amber); }

    /* ---- footer (shared chrome) ---- */
    .footer {
      max-width: var(--wrap); margin: 0 auto;
      padding: clamp(36px, 6vw, 56px) clamp(20px, 5vw, 40px);
      border-top: 1px solid var(--line-soft);
      display: flex; align-items: flex-start; justify-content: space-between;
      gap: 28px; flex-wrap: wrap;
    }
    .foot-brand { display: flex; gap: 14px; align-items: center; max-width: 380px; }
    .foot-brand img { border-radius: 10px; }
    .foot-name { font-family: var(--display); font-weight: 700; font-size: 1.1rem; }
    .foot-tag { color: var(--faint); font-size: 0.86rem; margin: 3px 0 0; line-height: 1.45; }
    .foot-links { display: flex; gap: 22px; }
    .foot-links a { font-family: var(--mono); font-size: 0.82rem; text-decoration: none; color: var(--muted); transition: color 0.18s; }
    .foot-links a:hover { color: var(--amber); }
    .foot-legal { width: 100%; font-family: var(--mono); font-size: 0.72rem; color: var(--faint); margin: 8px 0 0; }

    @keyframes rise { from { opacity: 0; transform: translateY(18px); } to { opacity: 1; transform: none; } }

    @media (max-width: 520px) {
      body { font-size: 16px; }
      .mb-read { display: none; }
      .mb-sec { display: none; }
      .rel-dl { flex-direction: column; align-items: stretch; max-width: none; }
    }

    :focus-visible { outline: 2px solid var(--amber); outline-offset: 3px; border-radius: 4px; }

    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after { animation: none !important; transition: none !important; scroll-behavior: auto; }
    }
  </style>
</body>
</html>
